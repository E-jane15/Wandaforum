import React from 'react';
import './Filters.css';

const Filters = () => {
  return (
    <div className="filters">
      <button>All (20)</button>
      <button>Archive (10)</button>
      <button>Favorites (17)</button>
    </div>
  );
};

export default Filters;
